import { BubbleSortDirective } from './bubble-sort.directive';

describe('BubbleSortDirective', () => {
  it('should create an instance', () => {
    const directive = new BubbleSortDirective();
    expect(directive).toBeTruthy();
  });
});
